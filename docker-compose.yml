services:

  backend:

    build: ./backend

    container_name: flask_backend

    ports:
      - "8080:8080"

    volumes:
      - ./backend:/app

    restart: always

  frontend:

    build: ./frontend

    container_name: react_frontend

    ports:
      - "3000:80"

    restart: always

    depends_on:
      - backend